"use client";

import Modal from "./Modal";

const PreviewModal : React.FC = () => {
  return (
    <section>
      <Modal
        open={p}
      >

      </Modal>
    </section>
  )
};

export default PreviewModal;